# Python
__pycache__/
*.pyc
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Virtual environments
.venv/
venv/
env/

# App/runtime artifacts
.streamlit/
.nicegui/

# Keep generated output in GitHub, but do not version raw uploads.

# Keep the generated dashboard output in GitHub.
# Evidence frames and the SQLite database are small enough to version,
# but raw source uploads are ignored because they can be very large.

# IDE files
.idea/
.vscode/

# OS files
Thumbs.db
Desktop.ini
